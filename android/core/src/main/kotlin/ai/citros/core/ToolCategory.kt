package ai.citros.core

/**
 * Tool grouping categories used for token-aware tool selection.
 */
enum class ToolCategory {
    CORE,
    NAVIGATION,
    INTERACTION,
    OBSERVATION,
    NOTIFICATION,
    CLIPBOARD,
    MEMORY,
    RESEARCH,
    PLANNING
}
