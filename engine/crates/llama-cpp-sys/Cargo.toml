[package]
name = "llama-cpp-sys"
version = "0.1.0"
edition = "2021"
authors = ["Nova Team"]
description = "Raw FFI bindings to llama.cpp for on-device LLM inference"
license = "MIT"
repository = "https://github.com/abbudjoe/nova"

[features]
default = []
llama-cpp = []  # Feature gate for actual llama.cpp integration

[build-dependencies]
cc = "1.0"

[dependencies]
# No runtime dependencies for raw FFI bindings
