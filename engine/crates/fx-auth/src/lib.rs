//! # fx-auth
//!
//! Authentication and OAuth primitives for Fawx LLM providers.
//! Provides credential/auth method management (`auth`) and PKCE OAuth flows
//! plus token parsing helpers (`oauth`).

pub mod auth;
pub mod oauth;
