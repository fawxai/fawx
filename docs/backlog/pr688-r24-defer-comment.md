Deferred follow-up (privacy-list persistence) remains out of PR #688 scope for the reasons captured in `docs/backlog/privacy-list-persistence.md`.

Backlog issue: https://github.com/abbudjoe/citros/issues/697

Rationale:
1. PR #688 is scoped to runtime privacy enforcement and redaction contracts.
2. Persistence policy requires backup/restore and startup UX decisions that cross app/runtime boundaries.
3. Acceptance criteria and required tests are listed in `docs/backlog/privacy-list-persistence.md`.
